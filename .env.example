# Anthropic API key — used only by the serverless function in api/, never shipped to the browser.
ANTHROPIC_API_KEY=

# Kill switch. When true, /api/extract serves pre-extracted example JSON and makes
# no upstream API call at all. Flip this if the key burns during the judging window.
DEMO_ONLY=false
